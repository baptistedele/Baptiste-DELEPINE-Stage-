sudo bash /usr/local/bin/batman-start.sh

#(cela permet de lancer le script BATMAN)

sudo batctl n

#(c'est une commande batctl qui permet de détecter les noeuds voisins)

iperf3 -s -p 4545

#(lance un test de transfert de paquets en tant que noeud listener (reception des paquets))

iperf3 -c 192.168.99.2 -p 4545 -u -b 10M -t 60  –get-server-output

#(lance un test de transfert de paquets en tant que noeud serveur (envoi des paquets))
